from setuptools import setup, find_packages

setup(
    name="PayPaython_mobile",
    version="1.0.0",
    packages=find_packages(),
    description="PayPaython mobile fork",
    author="nekomata723",
)
